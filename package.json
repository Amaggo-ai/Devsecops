{
  "name": "my-node-secure-app",
  "version": "0.1.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --runInBand",
    "security:audit": "npm audit --audit-level=moderate",
    "security:semgrep": "semgrep scan --config auto",
    "security:snyk": "snyk test"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}
